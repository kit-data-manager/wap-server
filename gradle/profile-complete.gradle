test {
    println 'Execute all tests...'
    outputs.dir snippetsDir
    include '**/*'
}

jacocoTestReport {
    dependsOn test
    reports {
        xml.required.set(true)
        html.required.set(false)
    }
}
